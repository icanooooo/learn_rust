mod collectionz;
use crate::collectionz::vectorz;
use crate::collectionz::stringz;
use crate::collectionz::hashmaps; 

fn main() {
    vectorz::vectors_example();

    stringz::strings_example();

    hashmaps::hashmaps_example();
}
